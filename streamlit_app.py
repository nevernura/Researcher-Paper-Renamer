"""Streamlit Cloud entry point for the PDF Renamer app.

The main implementation stays in app.py so existing local commands keep
working, while Streamlit Cloud can also auto-detect this conventional filename.
"""

import app  # noqa: F401
